Archived Proof Experiments
==========================

This directory keeps proof experiments that are no longer part of the active
`_CoqProject` theorem spine.

- `SmallStepCorrectnessBridge.v` records the old matched-trace bridge from
  terminal small-step runs to the terminating evaluator. The current paper
  skips adequacy and uses the direct continuation-machine theorem stack
  instead.
