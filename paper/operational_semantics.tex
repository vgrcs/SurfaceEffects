\section{Operational Semantics}
\label{sec:opsem}

The operational semantics has one primitive execution relation: the
continuation-machine step $s\longrightarrow s'\,;\,\eta$.  The other judgments
below are derived or auxiliary relations built around that step.  They are
kept distinct because they play different roles in the metatheory.

\begin{center}
\small
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{.18\linewidth}
                  >{\raggedright\arraybackslash}p{.33\linewidth}
                  >{\raggedright\arraybackslash}p{.39\linewidth}@{}}
\toprule
\textbf{Layer} & \textbf{Judgment} & \textbf{Role}\\
\midrule
machine step
  & $s\longrightarrow s'\,;\,\eta$
  & one continuation-machine step emitting one trace fragment\\
finite execution
  & $s\longrightarrow^{*}s'\,;\,\phi$
  & accumulated machine trace, represented as a list of actions\\
checked finite execution
  & $s\chksteps s'\,;\,\phi$
  & execution variant that treats failed pair-parallel prechecks as checked failure\\
scheduled pair-parallel run
  & $s\Longrightarrow_{\mathsf{sch}}^{\Phi_L,\Phi_R}s'$
  & finite run-phase scheduler built from branch machine steps\\
\bottomrule
\end{tabular}
\end{center}

The notation is intentionally separated.  Machine execution \emph{produces}
trace fragments; the finite-run relation concatenates them into a list trace
$\phi$. A one-step transition emits only a fragment $\eta$:
administrative steps emit $\Nil$, while heap operations emit singleton
fragments such as $\Alloc(l\at r)$, $\Read(l\at r)$, and $\Write(l\at r)$. The
trace of a longer execution is not stored in the ordinary state or continuation;
the pair-parallel runtime state temporarily stores the two branch traces so it
can perform the final checked trace-disjointness test.
Checked finite execution uses the same trace fragments, but changes the
observable outcome of a failed pair-parallel computed-effect precheck from
sequential fallback to checked failure.

\begin{formalbox}{Operational Domains}
\small
\renewcommand{\arraystretch}{1.2}
\begin{tabular*}{\linewidth}{@{}>{$}r<{$}@{\;}>{$}c<{$}@{\;}>{$}l<{$}@{\extracolsep{\fill}}l@{}}
E\in\mathit{Env}
  & \triangleq & \mathit{Variables}\rightharpoonup\mathit{Values}
  & variable assignment\\[.25ex]
P\in\mathit{Rgn}
  & \triangleq & \mathit{RgnVars}\rightharpoonup\mathit{RgnConst}
  & regions in scope\\[.25ex]
H\in\mathit{Heap}
  & \triangleq & \mathit{Addr}\rightharpoonup\mathit{Values}
  & heap assignment\\[.25ex]
\langle E,P,\varphi\rangle\in\mathit{Closures}
  & \triangleq & \mathit{Env}\times\mathit{Rgn}\times\mathit{Expressions}
  & function closures\\[.25ex]
v\in\mathit{Values}
  & \triangleq &
\begin{aligned}[t]
& \mathsf{eff}(\Theta)\mid (v_1,v_2)
  \mid n\mid b\mid ()\mid l\at r\mid \langle E,P,\varphi\rangle
\end{aligned}
  & dynamic values\\[.8ex]
s\in\mathit{State}
  & \triangleq &
\begin{aligned}[t]
& \langle H,E,P,e,k\rangle\mid\langle H,v,k\rangle\\[.25ex]
& \mid\mathsf{done}(H,v)
  \mid\mathsf{pairRun}(s_1,s_2,\phi_1,\phi_2,k)
  \mid\mathsf{error}(H)
\end{aligned}
  & machine states\\[.8ex]
k\in\mathit{Kont}
  & \triangleq &
\begin{aligned}[t]
& \mathsf{doneK}
  \mid K^\mu_f(e,E,P,k)
  \mid K^e_f(e,E,P,k)\\[.25ex]
& \mid K^\mu_a(E,P,f,x,e,e,k)
  \mid K^e_a(E,P,f,x,e,e,k)\\[.25ex]
& \mid K^{ref}(p,P,k)
  \mid K^{get}(p,P,k)
  \mid K^{set}(p,l,P,k)\\[.25ex]
& \mid K^{read}(k)
  \mid K^{write}(k)
  \mid K^{concat}(\Theta,k)\\[.25ex]
& \mid K^{\parallel}_1(\vec e,E,P,k)
  \mid K^{\parallel}_2(\vec e,E,P,\Theta,k)\\[.25ex]
& \mid K^{\parallel}_{F1}(e,E,P,k)
  \mid K^{\parallel}_{F2}(v,k).
\end{aligned}
  & continuations
\end{tabular*}
\end{formalbox}

The initial state for expression $e$ is the expression state with the final
continuation:
\[
\mathsf{init}(H,E,P,e)\triangleq
\langle H,E,P,e,\mathsf{doneK}\rangle.
\]

\begin{formalbox}{Finite Machine Executions}
\small
\noindent\textbf{Finite execution}\hfill $\boxed{s\longrightarrow^{*}s'\,;\,\phi}$

\rulepair{
\inferrule*[lab={[TR-Refl]}]{ }
  {s\longrightarrow^{*}s\,;\,\Nil}
}{
\inferrule*[lab={[TR-Step]}]
  {s\longrightarrow s_1\,;\,\eta \\
   s_1\longrightarrow^{*}s_2\,;\,\phi}
  {s\longrightarrow^{*}s_2\,;\,\eta\tracecat\phi}
}
\end{formalbox}

Each finite execution accumulates one-step fragments by induction; $\tracecat$
denotes list concatenation for machine traces and $\Nil$ is the empty trace.
We write $\phi\equiv_{\mathsf{perm}}\phi'$ when two list traces contain the same
actions up to permutation.
Ordinary expression evaluation is the special case
$\mathsf{init}(H,E,P,e)\longrightarrow^{*}\mathsf{done}(H',v)\,;\,\phi$.

The checked execution relation is the reflexive-transitive closure
of a checked one-step relation $s\chkstep s'\,;\,\eta$.  Away from
pair-parallel checking and pair-run states, checked steps agree with ordinary
machine steps. At a failed pair-parallel computed-effect precheck, ordinary
execution falls back to sequential evaluation, while checked execution reaches
the terminal checked failure state. In a pair-run state, checked branch steps
use $\chkstep$ recursively in the selected branch and otherwise perform the
same heap rebasing and trace accumulation as the ordinary run-phase rules. The
exceptional failed-precheck rule is displayed with the pair-parallel rules
below.

\begin{formalbox}{Checked Finite Machine Executions}
\small
\noindent\textbf{Checked finite execution}\hfill
  $\boxed{s\chksteps s'\,;\,\phi}$

\rulepair{
\inferrule*[lab={[Chk-Refl]}]{ }
  {s\chksteps s\,;\,\Nil}
}{
\inferrule*[lab={[Chk-Step]}]
  {s\chkstep s_1\,;\,\eta \\
   s_1\chksteps s_2\,;\,\phi}
  {s\chksteps s_2\,;\,\eta\tracecat\phi}
}
\end{formalbox}

The scheduled pair-run relation introduced below is finite in the same
inductive sense, but it is not another reflexive-transitive closure of the
ordinary global step relation. Each nonfinal scheduled rule consumes one
ordinary machine step from one branch and then recurses, so the relation
represents the possible interleavings of the two branch executions rather than
only the ordinary left-to-right machine order.

The continuation machine accumulates list traces, not views. When a theorem
must remember sequencing structure, we use the auxiliary judgment
$\mathsf{view}(s,\Phi,s')$, defined by the rules below. Its trace component is
recovered by the flattening map from Section~\ref{sec:effects}.

\begin{formalbox}{Trace-View Executions}
\small
\noindent\textbf{Structured finite execution}\hfill
  $\boxed{\mathsf{view}(s,\Phi,s')}$

\begin{rulemathpar}
\inferrule*[lab={[View-Refl]}]{ }
  {\mathsf{view}(s,\Nil,s)}
\end{rulemathpar}

\rulepair{
\inferrule*[lab={[View-Step]}]
  {s\longrightarrow s'\,;\,\eta}
  {\mathsf{view}(s,\eta,s')}
}{
\inferrule*[lab={[View-Seq]}]
  {\mathsf{view}(s_1,\Phi_1,s_2) \\
   \mathsf{view}(s_2,\Phi_2,s_3)}
  {\mathsf{view}(s_1,\Phi_1\tracecat\Phi_2,s_3)}
}
\end{formalbox}

Trace-view execution is a structured presentation of finite machine execution:
if $\mathsf{view}(s,\Phi,s')$, then
$s\longrightarrow^{*}s'\,;\,\mathsf{flatten}(\Phi)$; conversely, every finite
machine execution has a trace view with the same flattened trace. Thus
$\mathsf{flatten}$ appears at the boundary between view-level statements and
ordinary finite executions.

Terminal states $\mathsf{done}(H,v)$ and $\mathsf{error}(H)$ have no outgoing
machine step.  The error state is the checked-failure terminal used by checked
execution when a pair-parallel computed-effect precheck fails, and by the
ordinary run phase when the final branch-trace check fails.  Continuations are
bookkeeping devices for the call-by-value evaluation order.
A frame records the expression still to be evaluated, the environment and region
map needed to resume evaluation, and the continuation to resume afterward.
Application frames first remember the unevaluated argument while the function
position is evaluated, then remember the closure while the argument is
evaluated, and finally enter either the computational body or the surface-effect body
with the recursive name and argument bound.  Heap-operation frames remember the
pending operation and the region environment while a location or value
subexpression is evaluated.  The trace emitted while evaluating functions,
arguments, or surface-effect terms is the machine trace of that evaluation.  A computed
effect is the value returned by evaluating the corresponding surface-effect expression.

\begin{formalbox}{Small-Step Operational Semantics}
\small
\noindent\textbf{One machine step}\hfill $\boxed{s\longrightarrow s'\,;\,\eta}$
\ruletriple{
\inferrule*[lab={[S-Const]}]{ }
  {\langle H,E,P,\mathsf{const}\;n,k\rangle
   \longrightarrow
   \langle H,n,k\rangle\,;\,\Nil}
}{
\inferrule*[lab={[S-Var]}]{E(x)=v}
  {\langle H,E,P,x,k\rangle
   \longrightarrow
   \langle H,v,k\rangle\,;\,\Nil}
}{
\inferrule*[lab={[S-Done]}]{ }
  {\langle H,v,\mathsf{doneK}\rangle
   \longrightarrow
   \mathsf{done}(H,v)\,;\,\Nil}
}

\begin{rulemathpar}
\inferrule*[lab={[S-Clos]}]
  {\varphi=\mu f.\lambda x.\{e_b\mid e_s\}\;\vee\;\varphi=\Lambda\rho.e}
  {\langle H,E,P,\varphi,k\rangle
   \longrightarrow
   \langle H,\langle E,P,\varphi\rangle,k\rangle\,;\,\Nil}
\end{rulemathpar}

\begin{rulemathpar}
\inferrule*[lab={[S-Capp-Fun]}]{ }
  {\langle H,E,P,\capp e_f\,e_a,k\rangle
   \longrightarrow
   \langle H,E,P,e_f,K^\mu_f(e_a,E,P,k)\rangle\,;\,\Nil}
\end{rulemathpar}

\begin{rulemathpar}
\inferrule*[lab={[S-Capp-Arg]}]
  {v_f=\langle E',P',\mu f.\lambda x.\{e_b\mid e_s\}\rangle}
  {\langle H,v_f,K^\mu_f(e_a,E,P,k)\rangle
   \longrightarrow
   \langle H,E,P,e_a,K^\mu_a(E',P',f,x,e_b,e_s,k)\rangle\,;\,\Nil}
\end{rulemathpar}

\begin{rulemathpar}
\inferrule*[lab={[S-Capp-Body]}]
  {E''=E'[f\mapsto v_f][x\mapsto v_a] \\
   v_f=\langle E',P',\mu f.\lambda x.\{e_b\mid e_s\}\rangle}
  {\langle H,v_a,K^\mu_a(E',P',f,x,e_b,e_s,k)\rangle
   \longrightarrow
   \langle H,E'',P',e_b,k\rangle\,;\,\Nil}
\end{rulemathpar}

\begin{rulemathpar}
\inferrule*[lab={[S-Eapp-Fun]}]{ }
  {\langle H,E,P,\eapp e_f\,e_a,k\rangle
   \longrightarrow
   \langle H,E,P,e_f,K^e_f(e_a,E,P,k)\rangle\,;\,\Nil}
\end{rulemathpar}

\begin{rulemathpar}
\inferrule*[lab={[S-Eapp-Arg]}]
  {v_f=\langle E',P',\mu f.\lambda x.\{e_b\mid e_s\}\rangle}
  {\langle H,v_f,K^e_f(e_a,E,P,k)\rangle
   \longrightarrow
   \langle H,E,P,e_a,K^e_a(E',P',f,x,e_b,e_s,k)\rangle\,;\,\Nil}
\end{rulemathpar}

\begin{rulemathpar}
\inferrule*[lab={[S-Eapp-Body]}]
  {E''=E'[f\mapsto v_f][x\mapsto v_a] \\
   v_f=\langle E',P',\mu f.\lambda x.\{e_b\mid e_s\}\rangle}
  {\langle H,v_a,K^e_a(E',P',f,x,e_b,e_s,k)\rangle
   \longrightarrow
   \langle H,E'',P',e_s,k\rangle\,;\,\Nil}
\end{rulemathpar}

\begin{rulemathpar}
\inferrule*[lab={[S-Alloc]}]
  {P(p)=r \qquad \mathsf{fresh}(H,r)=l}
  {\langle H,v,K^{ref}(p,P,k)\rangle
   \longrightarrow
   \langle H[(r,l)\mapsto v],l\at r,k\rangle\,;\,\Alloc(l\at r)}
\end{rulemathpar}

\begin{rulemathpar}
\inferrule*[lab={[S-Get]}]
  {P(p)=r \qquad H(r,l)=v}
  {\langle H,l\at r,K^{get}(p,P,k)\rangle
   \longrightarrow
   \langle H,v,k\rangle\,;\,\Read(l\at r)}
\end{rulemathpar}

\begin{rulemathpar}
\inferrule*[lab={[S-Set]}]
  {P(p)=r \qquad H(r,l)\neq\bot}
  {\langle H,v,K^{set}(p,l,P,k)\rangle
   \longrightarrow
   \langle H[(r,l)\mapsto v],(),k\rangle\,;\,\Write(l\at r)}
\end{rulemathpar}
\end{formalbox}

\paragraph{Effect-valued expression steps.}
Surface-effect terms are ordinary expressions whose values are computed effects.
Abstract surface-effect forms return region-level actions without touching the heap;
concrete surface-effect forms first evaluate their argument and then return a concrete
computed action. These rules are ordinary machine steps and therefore emit
$\Nil$.

\begin{formalbox}{Effect-Valued Expression Steps}
\small
\noindent\textbf{One machine step}\hfill $\boxed{s\longrightarrow s'\,;\,\eta}$
\ruletriple{
\inferrule*[lab={[S-Abst-Read]}]{P(p)=r}
  {\langle H,E,P,\Rhat(p),k\rangle
   \longrightarrow
   \langle H,\Rs(r),k\rangle\,;\,\Nil}
}{
\inferrule*[lab={[S-Abst-Write]}]{P(p)=r}
  {\langle H,E,P,\What(p),k\rangle
   \longrightarrow
   \langle H,\Ws(r),k\rangle\,;\,\Nil}
}{
\inferrule*[lab={[S-Abst-Alloc]}]{P(p)=r}
  {\langle H,E,P,\Ahat(p),k\rangle
   \longrightarrow
   \langle H,\As(r),k\rangle\,;\,\Nil}
}

\ruletriple{
\inferrule*[lab={[S-Conc-Read]}]{ }
  {\langle H,l\at r,K^{read}(k)\rangle
   \longrightarrow
   \langle H,\Readhat(l\at r),k\rangle\,;\,\Nil}
}{
\inferrule*[lab={[S-Conc-Write]}]{ }
  {\langle H,l\at r,K^{write}(k)\rangle
   \longrightarrow
   \langle H,\Writehat(l\at r),k\rangle\,;\,\Nil}
}{
\inferrule*[lab={[S-Concat]}]{ }
  {\langle H,\Theta_2,K^{concat}(\Theta_1,k)\rangle
   \longrightarrow
   \langle H,\Theta_1\cup\Theta_2,k\rangle\,;\,\Nil}
}

\rulepair{
\inferrule*[lab={[S-Top]}]{ }
  {\langle H,E,P,\Top,k\rangle
   \longrightarrow
   \langle H,\top,k\rangle\,;\,\Nil}
}{
\inferrule*[lab={[S-Empty]}]{ }
  {\langle H,E,P,\Pure,k\rangle
   \longrightarrow
   \langle H,\emptyset,k\rangle\,;\,\Nil}
}
\end{formalbox}

The pair-parallel run phase uses the state
$\mathsf{pairRun}(s_1,s_2,\phi_1,\phi_2,k)$ to store two branch states, their
accumulated branch traces, and the continuation that receives the final pair value.

The checked pair-parallel form is operationally specialized to two computational
applications.  The corresponding surface-effect applications are evaluated
first.  If the computed-effect precheck succeeds, the ordinary machine enters a
pair-run state, runs the left branch to completion and then the right branch,
accumulating separate branch traces and checking them before returning the pair value.
If the computed-effect precheck fails, the ordinary machine falls back to
sequential evaluation of the two computational branches.  The checked execution
relation uses the same successful run phase, but replaces this sequential
fallback by terminal checked failure.

For compactness, write
\[
\begin{array}{rcl}
\vec e & \triangleq & (e_{f1},e_{a1},e_{f2},e_{a2})\\
e_i^\mu & \triangleq & \capp e_{fi}\,e_{ai}\\
e_i^e & \triangleq & \eapp e_{fi}\,e_{ai}.
\end{array}
\]
The run rules use two structural state operations.  If $s$ carries heap $H_0$,
then $\mathsf{heap}(s)=H_0$.  The state $s[H]$ is obtained from $s$ by replacing
only that carried heap by $H$.

\begin{formalbox}{Pair-Parallel Steps and Checked Precheck Failure}
\small
\noindent\textbf{One machine step}\hfill $\boxed{s\longrightarrow s'\,;\,\eta}$

\begin{rulemathpar}
\inferrule*[lab={[S-Pair-Start]}]{ }
  {\langle H,E,P,\pairpar{e_1^\mu}{e_2^\mu},k\rangle
   \longrightarrow
   \langle H,E,P,e_1^e,K^{\parallel}_1(\vec e,E,P,k)\rangle\,;\,\Nil}
\end{rulemathpar}

\begin{rulemathpar}
\inferrule*[lab={[S-Pair-Eff1]}]{ }
  {\langle H,\mathsf{eff}(\Theta_1),
      K^{\parallel}_1(\vec e,E,P,k)\rangle
   \longrightarrow
   \langle H,E,P,e_2^e,K^{\parallel}_2(\vec e,E,P,\Theta_1,k)\rangle\,;\,\Nil}
\end{rulemathpar}

\begin{rulemathpar}
\inferrule*[lab={[S-Pair-Check-OK]}]
  {\neg(\Theta_1 \effconflict \Theta_2)}
  {\langle H,\mathsf{eff}(\Theta_2),
      K^{\parallel}_2(\vec e,E,P,\Theta_1,k)\rangle
   \longrightarrow
   \mathsf{pairRun}(
      \mathsf{init}(H,E,P,e_1^\mu),
      \mathsf{init}(H,E,P,e_2^\mu),
      [\,],[\,],k)\,;\,\Nil}
\end{rulemathpar}

\begin{rulemathpar}
\inferrule*[lab={[S-Pair-Check-Fail]}]
  {\Theta_1 \effconflict \Theta_2}
  {\langle H,\mathsf{eff}(\Theta_2),
      K^{\parallel}_2(\vec e,E,P,\Theta_1,k)\rangle
   \longrightarrow
   \langle H,E,P,e_1^\mu,K^{\parallel}_{F1}(e_2^\mu,E,P,k)\rangle\,;\,\Nil}
\end{rulemathpar}

\rulepair{
\inferrule*[lab={[S-Pair-Fallback-L]}]{ }
  {\langle H,v_1,K^{\parallel}_{F1}(e_2^\mu,E,P,k)\rangle
   \longrightarrow
   \langle H,E,P,e_2^\mu,K^{\parallel}_{F2}(v_1,k)\rangle\,;\,\Nil}
}{
\inferrule*[lab={[S-Pair-Fallback-R]}]{ }
  {\langle H,v_2,K^{\parallel}_{F2}(v_1,k)\rangle
   \longrightarrow
   \langle H,(v_1,v_2),k\rangle\,;\,\Nil}
}

\begin{rulemathpar}
\inferrule*[lab={[C-Pair-Check-Fail]}]
  {\Theta_1 \effconflict \Theta_2}
  {\langle H,\mathsf{eff}(\Theta_2),
      K^{\parallel}_2(\vec e,E,P,\Theta_1,k)\rangle
   \chkstep
   \mathsf{error}(H)\,;\,\Nil}
\end{rulemathpar}

\begin{rulemathpar}
\inferrule*[lab={[S-Pair-Run-L]}]
  {s_1\longrightarrow s'_1\,;\,\eta}
  {\mathsf{pairRun}(s_1,s_2,\phi_1,\phi_2,k)
   \longrightarrow
   \mathsf{pairRun}(s'_1,
      s_2[\mathsf{heap}(s'_1)],
      \phi_1\tracecat\eta,\phi_2,k)\,;\,\eta}
\end{rulemathpar}

\begin{rulemathpar}
\inferrule*[lab={[S-Pair-Run-R]}]
  {s_2\longrightarrow s'_2\,;\,\eta}
  {\mathsf{pairRun}(\mathsf{done}(H_1,v_1),s_2,\phi_1,\phi_2,k)
   \longrightarrow
   \mathsf{pairRun}(\mathsf{done}(\mathsf{heap}(s'_2),v_1),
      s'_2,\phi_1,\phi_2\tracecat\eta,k)\,;\,\eta}
\end{rulemathpar}

\begin{rulemathpar}
\inferrule*[lab={[S-Pair-Done-OK]}]
  {\neg(\phi_1 \traceconflict \phi_2)}
  {\mathsf{pairRun}(\mathsf{done}(H,v_1),\mathsf{done}(H,v_2),\phi_1,\phi_2,k)
   \longrightarrow
   \langle H,(v_1,v_2),k\rangle\,;\,\Nil}
\end{rulemathpar}

\begin{rulemathpar}
\inferrule*[lab={[S-Pair-Done-Fail]}]
  {\phi_1 \traceconflict \phi_2}
  {\mathsf{pairRun}(\mathsf{done}(H,v_1),\mathsf{done}(H,v_2),\phi_1,\phi_2,k)
   \longrightarrow
   \mathsf{error}(H)\,;\,\Nil}
\end{rulemathpar}
\end{formalbox}

The ordinary semantics uses [S-Pair-Check-Fail] for a failed computed-effect
precheck. The checked relation uses [C-Pair-Check-Fail] in its place. Its
pair-run left and right rules are the checked counterparts of [S-Pair-Run-L]
and [S-Pair-Run-R], with $\chkstep$ in the branch premise; the state update,
heap rebasing, and emitted trace fragment are unchanged. The final pair-run
cases are shared by the ordinary and checked relations. If either branch
reaches $\mathsf{error}(H)$, the pair-parallel state steps to the same
checked-failure terminal state.

For the scheduler theorem, the same pair-run states are equipped with a derived
scheduled relation. It does not add a new source step; it allows either live
branch to take the next run-phase step while recording the two branch trace
views separately. The final scheduled cases coincide with the checked
pair-parallel run phase. For example, [Sch-Done-OK] has the same state-level
premise and conclusion as [S-Pair-Done-OK], but it is a base case of the finite
scheduled relation rather than one primitive machine step. Its branch views are
$\Nil,\Nil$ because the join itself emits no branch action; the accumulated
branch traces $\phi_1,\phi_2$ are already stored in the $\mathsf{pairRun}$
state.

\begin{formalbox}{Scheduled Pair-Parallel Runs}
\small
\noindent\textbf{Scheduled run}\hfill
  $\boxed{s\Longrightarrow_{\mathsf{sch}}^{\Phi_L,\Phi_R}s'}$

\rulepair{
\inferrule*[lab={[Sch-Err-L]}]{ }
  {\mathsf{pairRun}(\mathsf{error}(H),s_2,\phi_1,\phi_2,k)
   \Longrightarrow_{\mathsf{sch}}^{\Nil,\Nil}
   \mathsf{error}(H)}
}{
\inferrule*[lab={[Sch-Err-R]}]{ }
  {\mathsf{pairRun}(s_1,\mathsf{error}(H),\phi_1,\phi_2,k)
   \Longrightarrow_{\mathsf{sch}}^{\Nil,\Nil}
   \mathsf{error}(H)}
}

\rulepair{
\inferrule*[lab={[Sch-Done-OK]}]
  {\neg(\phi_1 \traceconflict \phi_2)}
  {\mathsf{pairRun}(\mathsf{done}(H,v_1),\mathsf{done}(H,v_2),\phi_1,\phi_2,k)
   \Longrightarrow_{\mathsf{sch}}^{\Nil,\Nil}
   \langle H,(v_1,v_2),k\rangle}
}{
\inferrule*[lab={[Sch-Done-Fail]}]
  {\phi_1 \traceconflict \phi_2}
  {\mathsf{pairRun}(\mathsf{done}(H,v_1),\mathsf{done}(H,v_2),\phi_1,\phi_2,k)
   \Longrightarrow_{\mathsf{sch}}^{\Nil,\Nil}
   \mathsf{error}(H)}
}

\begin{rulemathpar}
\inferrule*[lab={[Sch-Run-L]}]
  {s_1\longrightarrow s'_1\,;\,\eta \\
   \neg\exists H_e.\;s_2=\mathsf{error}(H_e) \\
   \mathsf{pairRun}(s'_1,s_2[\mathsf{heap}(s'_1)],
      \phi_1\tracecat\eta,\phi_2,k)
      \Longrightarrow_{\mathsf{sch}}^{\Phi_L,\Phi_R} s}
  {\mathsf{pairRun}(s_1,s_2,\phi_1,\phi_2,k)
      \Longrightarrow_{\mathsf{sch}}^{\eta\tracecat\Phi_L,\Phi_R} s}
\end{rulemathpar}

\begin{rulemathpar}
\inferrule*[lab={[Sch-Run-R]}]
  {s_2\longrightarrow s'_2\,;\,\eta \\
   \neg\exists H_e.\;s_1=\mathsf{error}(H_e) \\
   \mathsf{pairRun}(s_1[\mathsf{heap}(s'_2)],s'_2,
      \phi_1,\phi_2\tracecat\eta,k)
      \Longrightarrow_{\mathsf{sch}}^{\Phi_L,\Phi_R} s}
  {\mathsf{pairRun}(s_1,s_2,\phi_1,\phi_2,k)
      \Longrightarrow_{\mathsf{sch}}^{\Phi_L,\eta\tracecat\Phi_R} s}
\end{rulemathpar}
\end{formalbox}

Thus a left-branch step is the same state update as [S-Pair-Run-L]. A
right-branch step may occur before the left branch has finished; after such a
step the left state is rebased to the right branch's new heap. The side
conditions prevent either branch from continuing to schedule steps after the
other branch has already reached $\mathsf{error}(H)$. From empty branch
accumulators, the final stored branch traces are exactly
$\mathsf{flatten}(\Phi_L)$ and $\mathsf{flatten}(\Phi_R)$. The normalized
scheduler trace view is $\Phi_L\parallel\Phi_R$, whose flattening is the
canonical branch projection
$\mathsf{flatten}(\Phi_L)\tracecat\mathsf{flatten}(\Phi_R)$ even when the
scheduled execution interleaves the one-step fragments.

The connection back to finite machine executions is an embedding, not an
identification. Let
\[
s_0 =
\mathsf{pairRun}(
  \mathsf{init}(H,E,P,e_L^\mu),
  \mathsf{init}(H,E,P,e_R^\mu),
  [\,],[\,],\mathsf{doneK}).
\]
If the ordinary left-to-right machine run terminates,
\[
s_0\longrightarrow^{*}\mathsf{done}(H',v)\,;\,\phi,
\]
then there exist branch views $\Phi_L,\Phi_R$ such that
\[
s_0\Longrightarrow_{\mathsf{sch}}^{\Phi_L,\Phi_R}
  \langle H',v,\mathsf{doneK}\rangle
\qquad\text{and}\qquad
\phi\equiv_{\mathsf{perm}}
\mathsf{flatten}(\Phi_L\parallel\Phi_R).
\]
The converse is intentionally not stated as an ordinary finite execution:
arbitrary scheduled runs may take a right-branch step before the ordinary
machine would do so.

Surface-effect evaluation is proved heap-neutral from typing: if a well-typed
surface-effect execution emits a heap-neutral trace, the final heap is the
initial heap.  A surface effect may \emph{denote} an allocation through an
abstract allocation action without performing an allocation while the
surface-effect term itself is evaluated.
